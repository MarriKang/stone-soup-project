export default interface ProjectData {
    id: number,
    creatorId: number,
    title: string,
    goal: number,
    endDate: Date,
    description: string,
    status: string,
    imageURL: string,
    type: string
}